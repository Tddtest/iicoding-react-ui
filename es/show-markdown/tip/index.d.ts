import React, { ReactNode } from 'react';
import './index.less';
interface IProps {
    type?: string;
    children: ReactNode;
}
declare const Tip: React.NamedExoticComponent<IProps>;
export default Tip;
